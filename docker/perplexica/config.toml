[GENERAL]
PORT = 3000
SIMILARITY_MEASURE = "cosine"

[API_KEYS]
OPENAI = ""  # Not needed for local LLMs

[API_ENDPOINTS]
SEARXNG = "http://searxng:8080"
OLLAMA = "http://192.168.1.10:11434"

# Explicitly configure Ollama models to prevent auto-discovery issues
[CHAT_MODEL]
PROVIDER = "ollama"
MODEL = "llama3.1:8b"

[EMBEDDING_MODEL]
PROVIDER = "ollama"
MODEL = "nomic-embed-text"
